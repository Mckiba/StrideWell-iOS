//
//  ChatStubScreen.swift
//  Stridewell
//
//  Placeholder for M10 (ChatScreen — conversation UI, agent labels).
//

import SwiftUI

struct ChatStubScreen: View {
    var body: some View {
        ZStack {
            MapBackground()
            Text("Chat screen — Milestone 10")
                .foregroundStyle(.secondary)
        }
        .navigationTitle("Chat")
    }
}
